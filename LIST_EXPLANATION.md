# Списки (List) в Redis - Подробное объяснение

## Что такое список в Redis?

**Список (List)** - это упорядоченная коллекция строк в Redis. Элементы хранятся в порядке добавления и имеют индексы (позиции).

Представьте список как массив JavaScript или список Python:
- Элементы упорядочены (есть первый, второй, последний)
- Можно добавлять элементы в начало или конец
- Можно получать элементы по индексу
- Можно удалять элементы с начала или конца

## Ключевые особенности списков

1. **Упорядоченность** - элементы хранятся в порядке добавления
2. **Двусторонняя структура** - можно добавлять/удалять с обеих сторон
3. **Индексация** - каждый элемент имеет позицию (индекс)
4. **Дубликаты разрешены** - один элемент может встречаться несколько раз

## Визуальное представление

```
Список: tasks
┌─────┬─────┬─────┬─────┬─────┐
│  0  │  1  │  2  │  3  │  4  │  ← Индексы
├─────┼─────┼─────┼─────┼─────┤
│ "A" │ "B" │ "C" │ "D" │ "E" │  ← Элементы
└─────┴─────┴─────┴─────┴─────┘
  ↑                           ↑
Начало                    Конец
(HEAD)                    (TAIL)
```

## Аналогия: Очередь задач

Представьте список как очередь задач:
- **LPUSH** - добавить задачу в начало очереди (срочная задача)
- **RPUSH** - добавить задачу в конец очереди (обычная задача)
- **LPOP** - взять задачу из начала очереди (обработать первую)
- **RPOP** - взять задачу из конца очереди (обработать последнюю)

## Методы работы со списками

### 1. LPUSH - Добавить элементы в начало списка (слева)

```typescript
await redis.lpush('tasks', 'task1', 'task2', 'task3');
```

**Что происходит:**
- Элементы добавляются в начало списка (слева)
- Если список не существует, он создается
- Можно добавить несколько элементов за раз
- Порядок добавления сохраняется

**Пример:**
```typescript
// Начальное состояние: список пуст
await redis.lpush('tasks', 'A');
// Список: ['A']

await redis.lpush('tasks', 'B', 'C');
// Список: ['C', 'B', 'A']  ← новые элементы в начале
```

**Визуально:**
```
До:     ['A']
        ↓ LPUSH('B', 'C')
После:  ['C', 'B', 'A']
```

### 2. RPUSH - Добавить элементы в конец списка (справа)

```typescript
await redis.rpush('tasks', 'task1', 'task2', 'task3');
```

**Что происходит:**
- Элементы добавляются в конец списка (справа)
- Если список не существует, он создается
- Можно добавить несколько элементов за раз
- Порядок добавления сохраняется

**Пример:**
```typescript
// Начальное состояние: список пуст
await redis.rpush('tasks', 'A');
// Список: ['A']

await redis.rpush('tasks', 'B', 'C');
// Список: ['A', 'B', 'C']  ← новые элементы в конце
```

**Визуально:**
```
До:     ['A']
        ↓ RPUSH('B', 'C')
После:  ['A', 'B', 'C']
```

### 3. LPOP - Получить и удалить первый элемент (слева)

```typescript
const task = await redis.lpop('tasks');
// Вернет: 'C' (первый элемент)
// Список после: ['B', 'A']
```

**Что происходит:**
- Возвращается первый элемент списка (с индексом 0)
- Элемент удаляется из списка
- Если список пуст, возвращается `null`

**Пример:**
```typescript
// Список: ['C', 'B', 'A']
const first = await redis.lpop('tasks');
console.log(first); // 'C'
// Список после: ['B', 'A']
```

**Визуально:**
```
До:     ['C', 'B', 'A']
        ↓ LPOP()
После:  ['B', 'A']
        ↑
    Возвращено: 'C'
```

### 4. RPOP - Получить и удалить последний элемент (справа)

```typescript
const task = await redis.rpop('tasks');
// Вернет: 'A' (последний элемент)
// Список после: ['C', 'B']
```

**Что происходит:**
- Возвращается последний элемент списка
- Элемент удаляется из списка
- Если список пуст, возвращается `null`

**Пример:**
```typescript
// Список: ['C', 'B', 'A']
const last = await redis.rpop('tasks');
console.log(last); // 'A'
// Список после: ['C', 'B']
```

**Визуально:**
```
До:     ['C', 'B', 'A']
        ↓ RPOP()
После:  ['C', 'B']
        ↑
    Возвращено: 'A'
```

### 5. LRANGE - Получить диапазон элементов

```typescript
const items = await redis.lrange('tasks', 0, -1);
// Получить все элементы от начала до конца
```

**Параметры:**
- `start` - начальный индекс (0 - первый элемент, -1 - последний)
- `stop` - конечный индекс (включительно, -1 - до конца)

**Индексация:**
- Положительные индексы: 0, 1, 2, ... (от начала)
- Отрицательные индексы: -1, -2, -3, ... (от конца)
  - `-1` = последний элемент
  - `-2` = предпоследний элемент

**Примеры:**
```typescript
// Список: ['A', 'B', 'C', 'D', 'E']

// Получить все элементы
await redis.lrange('tasks', 0, -1);
// ['A', 'B', 'C', 'D', 'E']

// Получить первые 3 элемента
await redis.lrange('tasks', 0, 2);
// ['A', 'B', 'C']

// Получить последние 2 элемента
await redis.lrange('tasks', -2, -1);
// ['D', 'E']

// Получить элементы с индекса 1 до 3
await redis.lrange('tasks', 1, 3);
// ['B', 'C', 'D']
```

**Визуально:**
```
Список: ['A', 'B', 'C', 'D', 'E']
Индексы:  0   1   2   3   4
Отрицательные: -5  -4  -3  -2  -1

LRANGE(0, -1) → ['A', 'B', 'C', 'D', 'E']
LRANGE(0, 2)  → ['A', 'B', 'C']
LRANGE(-2, -1)→ ['D', 'E']
```

### 6. LLEN - Получить длину списка

```typescript
const length = await redis.llen('tasks');
// Вернет количество элементов в списке
```

**Что происходит:**
- Возвращает количество элементов в списке
- Если список не существует, возвращает 0

**Пример:**
```typescript
await redis.lpush('tasks', 'A', 'B', 'C');
const length = await redis.llen('tasks');
console.log(length); // 3
```

## Практические примеры использования

### Пример 1: Очередь задач (FIFO - First In, First Out)

```typescript
// Добавляем задачи в очередь
await redis.rpush('queue', 'task1');
await redis.rpush('queue', 'task2');
await redis.rpush('queue', 'task3');
// Очередь: ['task1', 'task2', 'task3']

// Обрабатываем задачи по порядку (первая пришла - первая обработана)
const task1 = await redis.lpop('queue'); // 'task1'
const task2 = await redis.lpop('queue'); // 'task2'
const task3 = await redis.lpop('queue'); // 'task3'
```

### Пример 2: Стек задач (LIFO - Last In, First Out)

```typescript
// Добавляем задачи в стек
await redis.lpush('stack', 'task1');
await redis.lpush('stack', 'task2');
await redis.lpush('stack', 'task3');
// Стек: ['task3', 'task2', 'task1']

// Обрабатываем задачи (последняя пришла - первая обработана)
const task3 = await redis.lpop('stack'); // 'task3'
const task2 = await redis.lpop('stack'); // 'task2'
const task1 = await redis.lpop('stack'); // 'task1'
```

### Пример 3: История действий пользователя

```typescript
// Добавляем действия в историю
await redis.lpush('user:1:history', 'viewed_page_A');
await redis.lpush('user:1:history', 'viewed_page_B');
await redis.lpush('user:1:history', 'viewed_page_C');
// История: ['viewed_page_C', 'viewed_page_B', 'viewed_page_A']

// Получаем последние 10 действий
const recentActions = await redis.lrange('user:1:history', 0, 9);
// ['viewed_page_C', 'viewed_page_B', 'viewed_page_A', ...]

// Ограничиваем историю 100 элементами
const historyLength = await redis.llen('user:1:history');
if (historyLength > 100) {
  // Удаляем старые элементы (с конца)
  await redis.rpop('user:1:history');
}
```

### Пример 4: Лента новостей

```typescript
// Добавляем новые посты в начало ленты
await redis.lpush('feed:user:1', 'post_123');
await redis.lpush('feed:user:1', 'post_124');
await redis.lpush('feed:user:1', 'post_125');
// Лента: ['post_125', 'post_124', 'post_123']

// Получаем первые 20 постов
const feed = await redis.lrange('feed:user:1', 0, 19);
```

### Пример 5: Очередь сообщений между сервисами

```typescript
class MessageQueue {
  constructor(private redis: RedisService) {}

  // Отправить сообщение
  async sendMessage(queueName: string, message: string) {
    await this.redis.rpush(queueName, message);
  }

  // Получить следующее сообщение
  async getNextMessage(queueName: string): Promise<string | null> {
    return await this.redis.lpop(queueName);
  }

  // Получить все сообщения (без удаления)
  async peekMessages(queueName: string, count: number): Promise<string[]> {
    return await this.redis.lrange(queueName, 0, count - 1);
  }

  // Получить размер очереди
  async getQueueSize(queueName: string): Promise<number> {
    return await this.redis.llen(queueName);
  }
}
```

## Сравнение LPUSH vs RPUSH

### LPUSH - Добавление в начало (как стек)
```
Начало: []
LPUSH('A') → ['A']
LPUSH('B') → ['B', 'A']
LPUSH('C') → ['C', 'B', 'A']
```
**Использование:** Стек, последние действия, новые элементы вверху

### RPUSH - Добавление в конец (как очередь)
```
Начало: []
RPUSH('A') → ['A']
RPUSH('B') → ['A', 'B']
RPUSH('C') → ['A', 'B', 'C']
```
**Использование:** Очередь, история в хронологическом порядке

## Сравнение LPOP vs RPOP

### LPOP - Взять из начала
```
Список: ['A', 'B', 'C']
LPOP() → 'A', список: ['B', 'C']
```
**Использование:** Обработка очереди (FIFO), если добавляли через RPUSH

### RPOP - Взять из конца
```
Список: ['A', 'B', 'C']
RPOP() → 'C', список: ['A', 'B']
```
**Использование:** Обработка стека (LIFO), если добавляли через LPUSH

## Паттерны использования

### 1. Очередь задач (FIFO)
```typescript
// Добавляем: RPUSH
await redis.rpush('queue', 'task1', 'task2');
// Обрабатываем: LPOP
const task = await redis.lpop('queue');
```

### 2. Стек (LIFO)
```typescript
// Добавляем: LPUSH
await redis.lpush('stack', 'item1', 'item2');
// Обрабатываем: LPOP
const item = await redis.lpop('stack');
```

### 3. История событий
```typescript
// Добавляем новые события в начало
await redis.lpush('history', 'event1', 'event2');
// Получаем последние N событий
const recent = await redis.lrange('history', 0, 9);
```

### 4. Ограничение размера списка
```typescript
// Добавляем элемент
await redis.lpush('list', 'new_item');
// Проверяем размер
const length = await redis.llen('list');
if (length > 100) {
  // Удаляем старые элементы с конца
  await redis.rpop('list');
}
```

## Важные особенности

### ✅ Преимущества списков:
- Быстрые операции добавления/удаления с концов (O(1))
- Упорядоченность элементов
- Поддержка дубликатов
- Гибкая индексация (положительные и отрицательные индексы)

### ⚠️ Ограничения:
- Доступ к элементу по индексу медленный (O(N))
- Не подходит для частого поиска по значению
- Все элементы должны быть строками

### 💡 Когда использовать списки:
- Очереди задач
- Стеки
- История действий
- Ленты новостей
- Любые упорядоченные коллекции, где важна последовательность

### ❌ Когда НЕ использовать списки:
- Нужен быстрый поиск по значению (используйте Set)
- Нужна сортировка по числовому значению (используйте Sorted Set)
- Нужны уникальные значения (используйте Set)

## Дополнительные операции (не реализованы в нашем сервисе, но доступны в Redis)

- `LINDEX` - получить элемент по индексу (без удаления)
- `LSET` - установить значение элемента по индексу
- `LINSERT` - вставить элемент до/после определенного значения
- `LREM` - удалить элементы по значению
- `LTRIM` - обрезать список, оставив только указанный диапазон
- `RPOPLPUSH` - атомарно переместить элемент из одного списка в другой
- `BLPOP/BRPOP` - блокирующие версии LPOP/RPOP (ожидают появления элемента)

## Итог

**Список в Redis** - это упорядоченная коллекция строк, идеально подходящая для:
- Очередей задач (FIFO)
- Стеков (LIFO)
- Истории событий
- Лент новостей
- Любых случаев, где важна последовательность элементов

Списки обеспечивают быстрые операции добавления/удаления с концов и гибкую работу с диапазонами элементов.

