# Отсортированные множества (Sorted Set) в Redis - Подробное объяснение

## Что такое отсортированное множество в Redis?

**Отсортированное множество (Sorted Set, ZSet)** - это структура данных в Redis, которая объединяет свойства множества (Set) и списка (List):
- **Как множество**: каждый элемент уникален (дубликаты невозможны)
- **Как список**: элементы отсортированы по числовой оценке (score)
- **Гибкость**: можно получать элементы по рангу (позиции) или по оценке

Представьте отсортированное множество как таблицу лидеров или рейтинг:
- Каждый участник (member) имеет оценку (score)
- Участники автоматически сортируются по оценке
- Можно быстро получить топ N участников
- Можно найти позицию участника в рейтинге

## Ключевые особенности отсортированных множеств

1. **Уникальность** - каждый элемент (member) может быть только один раз
2. **Сортировка** - элементы автоматически сортируются по оценке (score)
3. **Двойная индексация** - доступ по рангу (позиции) и по оценке
4. **Гибкие запросы** - можно получать элементы по диапазону рангов или оценок

## Визуальное представление

```
Отсортированное множество: leaderboard
┌──────┬──────────┬─────────┐
│ Ранг │ Участник │ Оценка  │
├──────┼──────────┼─────────┤
│  0   │ "Alice"  │  1000   │ ← Высший ранг (лучший)
│  1   │ "Bob"    │   850   │
│  2   │ "Charlie"│   750   │
│  3   │ "David"  │   600   │
│  4   │ "Eve"    │   500   │ ← Низший ранг
└──────┴──────────┴─────────┘
Сортировка: по убыванию оценки (от большего к меньшему)
```

## Аналогия: Таблица лидеров в игре

Представьте отсортированное множество как таблицу лидеров:
- **Member (участник)** - имя игрока
- **Score (оценка)** - количество очков
- **Rank (ранг)** - позиция в таблице (0 = первое место)
- Игроки автоматически сортируются по очкам
- Можно быстро получить топ-10 игроков

## Методы работы с отсортированными множествами

### 1. ZADD - Добавить элемент с оценкой

```typescript
await redis.zadd('leaderboard', 1000, 'Alice');
```

**Что происходит:**
- Добавляет элемент (member) с оценкой (score) в отсортированное множество
- Если элемент уже существует, его оценка обновляется
- Элементы автоматически сортируются по оценке
- Возвращает 1, если элемент добавлен, 0 - если обновлен

**Пример:**
```typescript
// Добавляем участников с их оценками
await redis.zadd('leaderboard', 1000, 'Alice');
await redis.zadd('leaderboard', 850, 'Bob');
await redis.zadd('leaderboard', 750, 'Charlie');
// Множество автоматически отсортировано: Alice (1000), Bob (850), Charlie (750)

// Обновляем оценку участника
await redis.zadd('leaderboard', 1100, 'Alice');
// Alice теперь с оценкой 1100, порядок обновился
```

**Визуально:**
```
До:     {}
        ↓ ZADD(1000, 'Alice')
После:  [Alice: 1000]

        ↓ ZADD(850, 'Bob')
После:  [Alice: 1000, Bob: 850]

        ↓ ZADD(1100, 'Alice')  (обновление)
После:  [Alice: 1100, Bob: 850]
```

### 2. ZRANGE - Получить диапазон элементов по рангу

```typescript
const topPlayers = await redis.zrange('leaderboard', 0, 9);
// Получить топ-10 игроков (первые 10 по рангу)
```

**Параметры:**
- `key` - имя ключа отсортированного множества
- `start` - начальный ранг (0 - первый элемент, -1 - последний)
- `stop` - конечный ранг (включительно, -1 - до конца)

**Что происходит:**
- Возвращает элементы в указанном диапазоне рангов
- Элементы отсортированы по оценке (от большего к меньшему)
- Возвращает только имена элементов (members), без оценок

**Примеры:**
```typescript
// Создаем таблицу лидеров
await redis.zadd('leaderboard', 1000, 'Alice');
await redis.zadd('leaderboard', 850, 'Bob');
await redis.zadd('leaderboard', 750, 'Charlie');
await redis.zadd('leaderboard', 600, 'David');
await redis.zadd('leaderboard', 500, 'Eve');

// Получить топ-3 игрока
const top3 = await redis.zrange('leaderboard', 0, 2);
console.log(top3); // ['Alice', 'Bob', 'Charlie']

// Получить всех игроков
const all = await redis.zrange('leaderboard', 0, -1);
console.log(all); // ['Alice', 'Bob', 'Charlie', 'David', 'Eve']

// Получить последних 2 игроков
const bottom2 = await redis.zrange('leaderboard', -2, -1);
console.log(bottom2); // ['David', 'Eve']
```

**Визуально:**
```
Ранг:   0      1       2        3      4
       Alice  Bob    Charlie  David  Eve
Оценка: 1000   850     750      600   500

ZRANGE(0, 2)   → ['Alice', 'Bob', 'Charlie']
ZRANGE(0, -1)  → ['Alice', 'Bob', 'Charlie', 'David', 'Eve']
ZRANGE(-2, -1) → ['David', 'Eve']
```

### 3. ZRANGEWITHSCORES - Получить диапазон элементов с их оценками

```typescript
const topWithScores = await redis.zrangeWithScores('leaderboard', 0, 2);
// Вернет: [{ member: 'Alice', score: 1000 }, { member: 'Bob', score: 850 }, ...]
```

**Что происходит:**
- Аналогично `ZRANGE`, но возвращает элементы вместе с их оценками
- Возвращает массив объектов с полями `member` и `score`
- Полезно, когда нужны не только имена, но и оценки

**Пример:**
```typescript
const top3 = await redis.zrangeWithScores('leaderboard', 0, 2);
console.log(top3);
// [
//   { member: 'Alice', score: 1000 },
//   { member: 'Bob', score: 850 },
//   { member: 'Charlie', score: 750 }
// ]

// Использование
top3.forEach((player, index) => {
  console.log(`${index + 1}. ${player.member}: ${player.score} очков`);
});
// 1. Alice: 1000 очков
// 2. Bob: 850 очков
// 3. Charlie: 750 очков
```

### 4. ZREM - Удалить элементы из отсортированного множества

```typescript
const removed = await redis.zrem('leaderboard', 'Alice', 'Bob');
// Удалит Alice и Bob из таблицы лидеров
```

**Что происходит:**
- Удаляет указанные элементы из отсортированного множества
- Если элемент не существует, он игнорируется
- Можно удалить несколько элементов за раз
- Возвращает количество удаленных элементов

**Пример:**
```typescript
// Таблица: [Alice: 1000, Bob: 850, Charlie: 750]
const removed = await redis.zrem('leaderboard', 'Alice', 'David');
console.log(removed); // 1 (удален только Alice, David не существовал)
// Таблица после: [Bob: 850, Charlie: 750]
```

## Практические примеры использования

### Пример 1: Таблица лидеров игры

```typescript
class LeaderboardService {
  constructor(private redis: RedisService) {}

  // Добавить/обновить результат игрока
  async updateScore(playerId: string, score: number) {
    await this.redis.zadd('leaderboard', score, playerId);
  }

  // Получить топ-10 игроков
  async getTopPlayers(count: number = 10) {
    const players = await this.redis.zrangeWithScores('leaderboard', 0, count - 1);
    return players.map((p, index) => ({
      rank: index + 1,
      playerId: p.member,
      score: p.score,
    }));
  }

  // Получить позицию игрока
  async getPlayerRank(playerId: string) {
    // ZRANK возвращает ранг (0-based), нужно добавить 1 для отображения
    const rank = await this.redis.client.zrank('leaderboard', playerId);
    return rank !== null ? rank + 1 : null;
  }

  // Получить оценку игрока
  async getPlayerScore(playerId: string) {
    return await this.redis.client.zscore('leaderboard', playerId);
  }
}

// Использование
const leaderboard = new LeaderboardService(redis);

await leaderboard.updateScore('player1', 1500);
await leaderboard.updateScore('player2', 1200);
await leaderboard.updateScore('player3', 1800);

const top10 = await leaderboard.getTopPlayers(10);
console.log(top10);
// [
//   { rank: 1, playerId: 'player3', score: 1800 },
//   { rank: 2, playerId: 'player1', score: 1500 },
//   { rank: 3, playerId: 'player2', score: 1200 }
// ]
```

### Пример 2: Рейтинг товаров по популярности

```typescript
// Увеличиваем популярность товара при просмотре
async function trackProductView(productId: string) {
  // Увеличиваем счетчик просмотров (используем текущую оценку + 1)
  const currentScore = await redis.client.zscore('products:popularity', productId);
  const newScore = (currentScore ? parseFloat(currentScore) : 0) + 1;
  await redis.zadd('products:popularity', newScore, productId);
}

// Получить самые популярные товары
async function getPopularProducts(count: number = 10) {
  const products = await redis.zrangeWithScores('products:popularity', 0, count - 1);
  return products.map(p => ({
    productId: p.member,
    views: p.score,
  }));
}

// Использование
await trackProductView('product:123');
await trackProductView('product:456');
await trackProductView('product:123'); // Второй просмотр

const popular = await getPopularProducts(5);
console.log(popular);
// [
//   { productId: 'product:123', views: 2 },
//   { productId: 'product:456', views: 1 }
// ]
```

### Пример 3: Система приоритетов задач

```typescript
// Добавляем задачи с приоритетами (чем больше число, тем выше приоритет)
await redis.zadd('tasks:priority', 10, 'task:critical:1');
await redis.zadd('tasks:priority', 5, 'task:normal:1');
await redis.zadd('tasks:priority', 8, 'task:high:1');
await redis.zadd('tasks:priority', 3, 'task:low:1');

// Получаем задачи в порядке приоритета (от высокого к низкому)
const highPriorityTasks = await redis.zrange('tasks:priority', 0, -1);
console.log(highPriorityTasks);
// ['task:critical:1', 'task:high:1', 'task:normal:1', 'task:low:1']

// Обновляем приоритет задачи
await redis.zadd('tasks:priority', 15, 'task:normal:1');
// Теперь task:normal:1 стал самым приоритетным
```

### Пример 4: Временные метки событий

```typescript
// Сохраняем события с временной меткой (timestamp как score)
const now = Date.now();
await redis.zadd('events:timeline', now, 'event:user:login');
await redis.zadd('events:timeline', now + 1000, 'event:user:action');
await redis.zadd('events:timeline', now + 2000, 'event:user:logout');

// Получаем события в хронологическом порядке
const events = await redis.zrange('events:timeline', 0, -1);
console.log(events);
// ['event:user:login', 'event:user:action', 'event:user:logout']

// Получаем события с временными метками
const eventsWithTime = await redis.zrangeWithScores('events:timeline', 0, -1);
eventsWithTime.forEach(event => {
  const date = new Date(event.score);
  console.log(`${event.member} в ${date.toISOString()}`);
});
```

### Пример 5: Рейтинг комментариев

```typescript
// Комментарии с рейтингом (лайки - дизлайки)
await redis.zadd('post:1:comments', 10, 'comment:1');  // 10 лайков
await redis.zadd('post:1:comments', 5, 'comment:2');   // 5 лайков
await redis.zadd('post:1:comments', -2, 'comment:3');  // -2 (больше дизлайков)

// Получаем лучшие комментарии (с положительным рейтингом)
const bestComments = await redis.zrange('post:1:comments', 0, 9);
console.log(bestComments);
// ['comment:1', 'comment:2'] (comment:3 не попал, так как отрицательный)

// Получаем комментарии с рейтингами
const commentsWithScores = await redis.zrangeWithScores('post:1:comments', 0, -1);
console.log(commentsWithScores);
// [
//   { member: 'comment:1', score: 10 },
//   { member: 'comment:2', score: 5 },
//   { member: 'comment:3', score: -2 }
// ]
```

## Сравнение с другими структурами данных

### Sorted Set vs Set (Множество)

| Характеристика | Sorted Set | Set |
|----------------|------------|-----|
| Уникальность | ✅ Да | ✅ Да |
| Сортировка | ✅ Да (по score) | ❌ Нет |
| Оценка элемента | ✅ Да (score) | ❌ Нет |
| Доступ по рангу | ✅ Да | ❌ Нет |
| Поиск по диапазону score | ✅ Да | ❌ Нет |
| Использование | Рейтинги, лидерборды | Теги, уникальные коллекции |

### Sorted Set vs List (Список)

| Характеристика | Sorted Set | List |
|----------------|------------|------|
| Уникальность | ✅ Да | ❌ Нет |
| Сортировка | ✅ Автоматическая (по score) | ✅ По порядку добавления |
| Оценка элемента | ✅ Да (score) | ❌ Нет |
| Изменение порядка | ✅ Автоматически при изменении score | ❌ Требует пересоздания |
| Использование | Рейтинги, приоритеты | Очереди, история |

## Дополнительные операции (доступны через прямой доступ к клиенту)

В нашем сервисе реализованы основные операции. Дополнительные операции можно использовать через `redis.client`:

### ZRANK - Получить ранг элемента

```typescript
const rank = await redis.client.zrank('leaderboard', 'Alice');
// Вернет позицию Alice в таблице (0-based, null если не найдена)
```

### ZSCORE - Получить оценку элемента

```typescript
const score = await redis.client.zscore('leaderboard', 'Alice');
// Вернет оценку Alice (строка) или null
```

### ZREVRANGE - Получить диапазон в обратном порядке

```typescript
const bottom = await redis.client.zrevrange('leaderboard', 0, 9);
// Вернет последние 10 элементов (от меньшего к большему)
```

### ZCOUNT - Подсчитать элементы в диапазоне оценок

```typescript
const count = await redis.client.zcount('leaderboard', 500, 1000);
// Вернет количество элементов с оценкой от 500 до 1000
```

### ZINCRBY - Увеличить оценку элемента

```typescript
await redis.client.zincrby('leaderboard', 100, 'Alice');
// Увеличит оценку Alice на 100
```

### ZRANGEBYSCORE - Получить элементы по диапазону оценок

```typescript
const players = await redis.client.zrangebyscore('leaderboard', 500, 1000);
// Вернет всех игроков с оценкой от 500 до 1000
```

## Паттерны использования

### 1. Таблица лидеров

```typescript
// Обновление счета
await redis.zadd('leaderboard', newScore, playerId);

// Получение топ-10
const top10 = await redis.zrangeWithScores('leaderboard', 0, 9);

// Получение позиции игрока
const rank = await redis.client.zrank('leaderboard', playerId);
```

### 2. Приоритетная очередь

```typescript
// Добавление задач с приоритетом
await redis.zadd('tasks', priority, taskId);

// Получение задач в порядке приоритета
const tasks = await redis.zrange('tasks', 0, -1);
```

### 3. Временная шкала

```typescript
// Сохранение событий с timestamp
await redis.zadd('timeline', Date.now(), eventId);

// Получение событий в хронологическом порядке
const events = await redis.zrange('timeline', 0, -1);
```

### 4. Рейтинговая система

```typescript
// Обновление рейтинга
await redis.zadd('ratings', rating, itemId);

// Получение лучших элементов
const top = await redis.zrangeWithScores('ratings', 0, 9);
```

## Важные особенности

### ✅ Преимущества отсортированных множеств:
- **Автоматическая сортировка** - элементы всегда отсортированы
- **Уникальность** - дубликаты невозможны
- **Гибкие запросы** - доступ по рангу и по оценке
- **Эффективность** - быстрые операции даже на больших данных
- **Обновление оценок** - можно обновлять оценку, порядок обновится автоматически

### ⚠️ Ограничения:
- Все оценки должны быть числами
- Порядок сортировки фиксирован (по оценке)
- Нельзя хранить сложные объекты (только строки как members)

### 💡 Когда использовать отсортированные множества:
- Таблицы лидеров
- Рейтинги и рейтинговые системы
- Приоритетные очереди
- Временные шкалы (timeline)
- Сортировка по любому числовому критерию
- Топ-N запросы

### ❌ Когда НЕ использовать отсортированные множества:
- Нужна сортировка по нечисловому критерию (используйте List)
- Нужны дубликаты (используйте List)
- Нужна простая уникальная коллекция без сортировки (используйте Set)

## Итог

**Отсортированное множество в Redis** - это уникальная структура данных, объединяющая:
- Уникальность множества
- Сортировку списка
- Гибкие запросы по рангу и оценке

Идеально подходит для:
- Таблиц лидеров
- Рейтинговых систем
- Приоритетных очередей
- Временных шкал
- Любых случаев, где нужна сортировка по числовому значению

Отсортированные множества обеспечивают автоматическую сортировку и эффективные запросы даже на больших объемах данных.

